[' * @param Hash pluginOptions Options for detecting/updating the Flash plugin (optional).', ' * @param Function replace Custom block called for each matched element if flash is installed (optional).', " * @param Function update Custom block called for each matched if flash isn't installed (optional).", " * @desc Embed a Flash movie, don't show an update message if Flash isn't installed.", '\t\t// Use Express Install (if specified and Flash plugin 6,0,65 or higher is installed).', '\t\t\t// see: http://blog.deconcept.com/2006/01/11/getvariable-setvariable-crash-internet-explorer-flash-6/', "\t\treturn new ActiveXObject('ShockwaveFlash.ShockwaveFlash').GetVariable('$version').replace(/\\D+/g, ',').match(/^,?(.+),?$/)[1];", "\tvar msg = 'This content requires the Flash Player. Download Flash Player. Already have Flash Player? Click here.';", ' * @desc Convert a hash of html options to a string of attributes, using Function.apply(). ', ' * @desc Convert a hash of flashvars to a url-encoded string, using Function.apply(). ', ' * Flash Player 9 Fix (http://blog.deconcept.com/2006/07/28/swfobject-143-released/)']