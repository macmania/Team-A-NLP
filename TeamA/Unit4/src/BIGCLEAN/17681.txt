['\t      // animate to target and set the hash to the window.location after the animation', "\t      $('html:not(:animated),body:not(:animated)').animate({ scrollTop: target }, duration, easing, function() {", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", "\t    else if (topOrBottomOrBoth == 'bottom') {} // bottom part of element is visible", '\t\t$("body").stop().animate({backgroundColor: backgroundColors[currentID]}, 600);', '\t\tif(cindex != activeindex) // if current node in view is selected again skip it', '\t\t\tif(activenode=="introVideos"){cnode = introVideos.cnode} else if (activenode=="aboutSlide") {cnode = aboutSlide.cnode} else if (activenode=="pressSlide") {cnode = pressSlide.cnode} else if (activenode=="viewadsSlide") {cnode = viewadsSlide.cnode}; // need one for each node :\\', '\t\t\t{\tif(cnode == ".p"){nnode=".c";pnode=".n";} else if (cnode == ".n"){nnode=".p";pnode=".c";} else { nnode=".n";pnode=".p";}', '\t\t\t\tif($(content).html() !=  $("."+activenode+" "+nnode+" > div").html()){ // don\'t clone node if it\'s already in next position', '\t\t\t\tif(cnode == ".c"){ cnode=".n"} else if (cnode == ".p"){cnode=".c";} else {cnode=".p";}', '\t\t\t\tif(cnode == ".c"){ nnode=".n";pnode=".p";} else if (cnode == ".n"){nnode=".p";pnode=".c";} else {nnode=".c";pnode=".n";}', '\t\t\t\tif($(content).html() !=  $("."+activenode+" "+pnode+" > div").html()){ // don\'t clone node if it\'s already in next position', '\t\t\t\t$("."+activenode+" "+cnode).animate({"left": $("."+activenode+" "+cnode).width()}, "slow");', '\t\t\t\t$("."+activenode+" "+nnode).attr("style", function() { return "position:absolute;left:-"+pw+"px;";\t});', '\t\t\t\tif(cnode == ".c"){ cnode=".p"} else if (cnode == ".p"){cnode=".n";} else {cnode=".c";}', '\t\t\tif(activenode=="introVideos"){introVideos.cnode = cnode} else if (activenode=="aboutSlide") {aboutSlide.cnode = cnode} else if (activenode=="pressSlide") {pressSlide.cnode = cnode} else if (activenode=="viewadsSlide") {viewadsSlide.cnode = cnode}; // need one for each node :\\', '\t\t\tvar hitindex=$(this).parent().children().index(this);\t// get index position of clicked element']